# Android app

The plan of record for putting sportydolphin.fun on Google Play. Started Aug 21, 2026.
Nothing here has shipped yet; the "Status" line under each step says where it actually is.

## What we are building, and what we are not

A **Trusted Web Activity**: an Android app whose entire UI is Chrome rendering
`https://sportydolphin.fun` full-screen, with no URL bar and no browser chrome. It is
generated by [Bubblewrap](https://github.com/GoogleChromeLabs/bubblewrap) from the live
`manifest.webmanifest`, so the app has no copy of the site in it.

**This means the web deploy IS the app deploy.** A push to `main` updates what every
installed app renders, with no Play review and no version bump. The `.aab` only has to be
rebuilt when something *outside* the web content changes: the app name, the icon, the
package id, the target SDK, or the Bubblewrap config. Budget roughly one rebuild a year for
Play's target-API deadline, and otherwise none.

**Not Capacitor, and this is the load-bearing reason:** Google blocks OAuth sign-in inside
embedded WebViews, and `AuthContext.signInWithGoogle` is exactly that flow. A TWA is real
Chrome with the real Chrome cookie jar, so sign-in works untouched and a user already signed
in on the web is already signed in here. In a Capacitor WebView the same code fails, and the
fix is a native plugin plus a deep-link round trip, which is a genuine rewrite of the auth
path. Go to Capacitor only for something the web cannot do at all: home-screen widgets, real
background execution, FCM-native push.

## The web side

These are done, and they are the parts that belong in this repo.

### 1. Manifest

**Status: done, no change needed.** `public/manifest.webmanifest` already carries everything
Bubblewrap reads. `src/__tests__/pwaShell.test.ts` pins the fields the Android build depends
on, since none of them can fail visibly on the web.

One thing that looks like a bug and must not be "fixed": `id` is `/mlb` while `start_url` is
`/wpbl`. `id` is the app's identity, not a route. Chrome keys an installed PWA on it, and
once this is on Play the Digital Asset Links association is built against it too. Changing it
does not rename the app, it creates a different one and orphans every existing install. The
mismatch is cosmetic and free; the fix is not.

### 2. Offline page

**Status: done.** `public/offline.html`, served by `public/sw.js` when a navigation cannot
reach the network. On the web this barely mattered, because a dead network is the browser's
problem and the browser says so in its own chrome. In a TWA there is no browser chrome, so
the identical failure renders Chrome's dinosaur full-screen under our icon and our app name,
which reads as a broken app rather than a bad tunnel.

The service worker still does **not** cache the app shell, and must not start. Only
`offline.html` and the three static files it draws are precached. Navigations are network
first with nothing written back, so there is no way to serve a stale app.

**The trap this hit on the first deploy, Aug 21, 2026:** Cloudflare Pages canonicalises
`/offline.html` to `/offline` with a 308. `Cache.put` rejects a redirected response, and
`cache.addAll` is atomic, so that one redirect threw away the entire precache. Nothing
errored: the worker installed, push kept working, and the only symptom would have been an
offline fallback that never appeared on a phone. `npm run dev` cannot show this, because
Vite serves the file at its real path with no canonicalisation. The precache now fetches
each asset separately and rebuilds the body into a clean 200 keyed on the original URL.
Pinned in `pwaShell.test.ts`.

The general lesson, which is the same one `_redirects` teaches: **any URL this project hard-
codes should be checked against the deployed site, not the dev server.** Pages rewrites more
than it looks like it does.

### 3. Digital Asset Links

**Status: half done. The upload key is in; Google's is not, and cannot be until the first
upload.**

`https://sportydolphin.fun/.well-known/assetlinks.json` is what proves the app and the domain
are the same owner. Without it the TWA still installs and runs, but Chrome shows the URL bar
across the top of every screen, which is the single most obvious "this is a website in a
costume" tell.

The file lives at `public/.well-known/assetlinks.json` and is pinned by
`src/__tests__/pwaShell.test.ts`, since JSON cannot carry its own comments. The dotted
directory survives `npm run build` into `dist/`, and nothing shadows it: `_routes.json` only
gates the Pages Functions, and `_redirects` has no `/*` rule.

It must end up listing **two** fingerprints, and stopping at one is the most common way
people ship a TWA with a visible URL bar:

1. the **upload key**, the `android.keystore` Bubblewrap generated. **Done**, added
   Aug 21, 2026.
2. the **app signing key**, which Google generates and holds under Play App Signing. Its
   fingerprint does not exist until the first bundle has been uploaded. Read it from Play
   Console under Release ▸ Setup ▸ App integrity and paste it in as a second entry.

**The trap in between:** with only the upload key listed, the sideloaded
`app-release-signed.apk` verifies fine, because that is the key it was signed with. The build
Play serves is re-signed with Google's key and will show the URL bar. So testing the APK on a
device proves nothing about the version users get, and the symptom only appears after
release.

Verify a deployed change with Google's own checker rather than by eye, since a cached or
mis-typed file fails silently:

```
https://digitalassetlinks.googleapis.com/v1/statements:list?source.web.site=https://sportydolphin.fun&relation=delegate_permission/common.handle_all_urls
```

## The Android side

None of this lives in this repo, deliberately. The Bubblewrap project is a sibling directory,
`../sportydolphin-android`, so that `android.keystore` can never be committed next to tracked
files. It carries its own `.gitignore` excluding the keystore, the build output and
`local.properties`, in case it is ever turned into a repo of its own.

### Prerequisites

Bubblewrap wants a JDK and the Android SDK build tools and will offer to download and manage
its own copies on first run. Let it. This machine has a system JDK 18 on PATH, which is not
the version Bubblewrap pins, and pointing it at the wrong JDK produces build errors that read
as Gradle problems.

```bash
npm install -g @bubblewrap/cli
```

### Generate the project

```bash
bubblewrap init --manifest=https://sportydolphin.fun/manifest.webmanifest
```

It prompts for the things that are **frozen the moment the first build reaches Play**:

- **Application ID** (package name). Reverse-DNS, and permanent: it cannot be changed, ever,
  without shipping a different app and losing every install and review. `fun.sportydolphin.app`
  is the obvious choice. Note it is unrelated to `manifest.id`.
- **App name / launcher name.** The launcher name has to be short or Android truncates it.
- **Signing key.** Bubblewrap offers to create the keystore and asks for passwords. Run this
  step yourself: those passwords are the one secret in this project that cannot be rotated.
  Losing the keystore is survivable under Play App Signing (Google can reset the upload key);
  losing it *before* the first upload is not.
- **Push notifications: say yes.** This adds the notification delegation service, without
  which Web Push silently does not reach the app.

### Build

```bash
bubblewrap build
```

Produces `app-release-bundle.aab` for Play and `app-release-signed.apk` for sideloading onto
a device to test.

### Get the upload fingerprint

`bubblewrap fingerprint list` is **not** it: that subcommand manages *additional* fingerprints
you register by hand, and prints nothing on a fresh project. The keystore's own fingerprint
comes from keytool, in the JDK Bubblewrap installed for itself:

```bash
& "$env:USERPROFILE\.bubblewrap\jdk\jdk-17.0.11+9\bin\keytool.exe" -list -v -keystore android.keystore -alias android
```

Let it prompt for the password rather than passing `-storepass`, which would put the one
unrotatable secret in this project into shell history.

### What the generated project settled on

Recorded here because `twa-manifest.json` lives outside this repo, in a sibling directory
(`../sportydolphin-android`) so that `android.keystore` can never be committed next to
tracked files.

| Key | Value | Note |
|---|---|---|
| `packageId` | `fun.sportydolphin.app` | Permanent. Must match `assetlinks.json`. |
| `startUrl` | `/wpbl` | |
| `fullScopeUrl` | `https://sportydolphin.fun/` | The whole site, so no tap escapes to a browser tab. |
| `enableNotifications` | `true` | Without it Web Push silently never arrives. |
| `monochromeIconUrl` | `/badge-96.png` | Android keeps only the alpha for a notification icon, and this file is already the dolphin cut out on transparency. |
| `launcherName` | `sportydolphin` | Bubblewrap's prompt caps this at 12 characters and had silently truncated it to `sportydolphi`, which reads as a typo. The cap is Bubblewrap's, not Android's. **Re-check this after any `bubblewrap update`.** |
| `orientation` | `portrait-primary` | Inherited from the web manifest. Wide stat tables argue for `default`. |

`bubblewrap update` bumps the version on every run, so it will silently walk `versionCode`
forward if run repeatedly before a release.

## What to test on a real device

The unit tests cover the manifest and the worker. These are the things that can only fail on
a phone:

1. **No URL bar.** If one appears, assetlinks is wrong, or the app was signed with a key
   whose fingerprint is not in the file, or the file is not being served as JSON.
2. **Google sign-in end to end.** Expected to work, because it is Chrome underneath, but this
   is the highest-consequence path in the app and the assumption should be checked rather
   than trusted.
3. **Push.** Two separate things have to be true: notification delegation is compiled in, and
   Android 13+ has been granted the runtime `POST_NOTIFICATIONS` permission, which the web
   permission prompt does not by itself raise. Verify a notification actually lands, and that
   tapping it opens the right screen (`notificationclick` in `sw.js` hands the target URL to
   the page).
4. **Offline.** Turn on airplane mode and cold-start the app. Expect `offline.html`, not a
   dinosaur.
5. **Back button** at the root of the app, and the deep-link handoff: with assetlinks live, a
   `sportydolphin.fun` link tapped anywhere on the device should open the app.

## Play Console

- **$25, one time.** The money is not the constraint.
- **The constraint is the testing requirement.** A *personal* developer account registered
  after Nov 13, 2023 must run a closed test with **at least 12 testers opted in continuously
  for 14 days** before it can even apply for production access. That is a two week wall with
  twelve real Google accounts behind it, and it is the reason this has a lead time rather than
  an afternoon. Organisation accounts are exempt but need a D-U-N-S number.
- **Listing assets needed:** feature graphic 1024x500, at least two phone screenshots, short
  and full description, an app icon at 512x512 (`public/icon-512.png` already qualifies), and
  a privacy policy URL, which `/privacy` satisfies.
- **Data safety form.** It must match reality: the app collects an email address and a Google
  account id through Supabase Auth, and site analytics through the `events` table. Declaring
  less than is collected is the kind of thing that gets an app pulled.

## iOS, later

There is no TWA equivalent. iOS means Capacitor or a WKWebView wrapper, which means the auth
rewrite described at the top, and then Apple's App Store Review guideline 4.2, which exists
specifically to reject apps that are a website in a wrapper. It needs native functionality to
pass. Treat it as a separate project with a real risk of rejection, not as the second half of
this one.
