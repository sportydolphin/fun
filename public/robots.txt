# sportydolphin.fun — allow everything, point crawlers at the sitemap.
User-agent: *
Allow: /

# Owner-only dashboard. Nothing here is a security boundary — the data behind it is gated
# server-side by RLS and the is_site_owner() checks in the admin RPCs — this just keeps a
# sign-in wall out of the index.
Disallow: /admin

# The fan awards ballot, which renders for the owner only while it is being built. Same
# reasoning as the line above, and the same non-guarantee: the route answers 200 for anybody,
# it just has nothing in it for them. Remove this when the ballot opens to everyone.
Disallow: /wpbl/awards

Sitemap: https://sportydolphin.fun/sitemap.xml
